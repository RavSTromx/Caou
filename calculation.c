#include <stdio.h>

int main()
{
	int x, y;
	char e;

	printf("두 수를 입력하시오:");
	scanf("%d %d", &x, &y);

	printf("연산자를 입력하시오:");
	scanf("%c", &e);

	if (c == '+')
		printf("%d+%d=%d", a, b, a + b);
}